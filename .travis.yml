language: ruby

addons:
  postgresql: "9.4"

services:
  - postgresql

before_script:
  - bundle exec rake db:create
  - bundle exec rake db:migrate

branches:
    only:
        - master

script:
  - git branch
  - bundle exec rake

bundler_args: "--without development"
cache:
  - bundler

notifications:
  email: false
