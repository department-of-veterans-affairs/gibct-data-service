<% @updated_rate_adjustments.each do |rate_adjustment| %>
  <%= turbo_stream.replace dom_id(rate_adjustment, :rate_input) do %>
    <input type="number"
      min="0.00"
      step="0.01"
      required
      disabled
      id="<%= dom_id(rate_adjustment, :rate_input)%>"
      name="rate_adjustments[<%= rate_adjustment.id %>][rate]"
      data-rates-form-target="input"
      value="<%= decimalize(rate_adjustment.rate) %>" />
  <% end %>

  <!--<%= turbo_stream.replace dom_id(rate_adjustment, :rate_select) do %>
  <% end %>-->
<% end %>
