<h1>Raw File Sources</h1>
<div class="row row-space-6">
	<div class="col-xs-8">
		These are the allowed raw file sources for the GIBCT. These sources reflect
		the various sources from which the GIBCT data is derived, for example:
		WEAMS.
	</div>

	<div class="col-xs-4">
		<% if list_link %>
			<div class="row row-space-6">
				<div class="col-xs-12">
					<%= link_to 'List Raw File Sources', 
						raw_file_sources_path, class: "btn btn-success btn-xs", 
						role: "button" %>
				</div>
			</div>
		<% end %>

		<% if edit_link %>
			<div class="row row-space-6">
				<div class="col-xs-12">		
					<%= link_to "Edit #{@raw_file_source.name}", 
						edit_raw_file_source_path(@raw_file_source), 
						class: "btn btn-primary btn-xs", 
						role: "button" %>
				</div>
			</div>
		<% end %>

		<% if new_link %>
			<div class="row row-space-6">
				<div class="col-xs-12">
					<%= link_to 'New Raw File Source', 
						new_raw_file_source_path, class: "btn btn-primary btn-xs", 
						role: "button" %>
				</div>
			</div>
		<% end %>
	</div>
</div>
	