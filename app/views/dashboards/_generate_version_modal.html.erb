<div data-generate-version-modal-target="dialog"
     class="modal fade"
     role="dialog">
  <div class="modal-dialog" id="generate_version_modal">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal">&times;</button>
        <h4 class="modal-title">Generate New Version and Publish</h4>
      </div>
      <div class="modal-body">
        <% if @latest_uploads.length > 0 %>
          <strong>Are you sure you want to generate and publish a new version?</strong>
          <p>Below are the CSVs that have been uploaded since last preview generation</p>
          <div class="table-wrapper">
            <table class="table table-hover table-condensed table-bordered table-responsive">
              <thead>
                <tr>
                  <th>CSV Type</th>
                  <th>File Name</th>
                  <th>Upload Date</th>
                  <th>Uploaded By</th>
                  <th>Comments</th>
                </tr>
              </thead>
              <tbody>
              <%= render partial: 'latest_upload_dialog', collection: @latest_uploads, as: :upload %>
              </tbody>
            </table>
          </div>
        <% else %>
          <p>No new data files have been uploaded since the last preview generation</p>
        <% end %>
        <%= link_to 'Generate Version', dashboard_build_path, method: :post,
                    class: "btn btn-success",
                    role: "button" %>
      </div>
    </div>
  </div>
</div>