<div class="row row-space-6">
  <div class="col-xs-9">
    <p class="lead">
      These are the CSV files from which the database will be constructed.
      As the CSV is uploaded, a validation on every row in the CSV will ensure
      that there are no issues with the data. If successful, the uploaded data
      can be used to construct the Data.csv. Data may be uploaded in any desired
      order, and once all new CSV data has been uploaded, a preview version
      of the GI Bill Comparison Tool database may be built. If you are satisfied
      that the data is correct, you may "push" the preview version of the
      database to production.
    </p>
  </div>

  <div class="col-xs-3">
    <% if action_name == 'index' %>
      <div class="row row-space-6">
        <div class="col-xs-12">
           <%= link_to 'Upload a File', new_upload_path,
               class: "btn btn-success btn-xs", role: "button", style: "width: 70%" %>
        </div>
      </div>

      <div class="row row-space-6">
        <div class="col-xs-12">
           <%= link_to 'View All Uploads', uploads_path,
               class: "btn btn-info btn-xs", role: "button", style: "width: 70%" %>
        </div>
      </div>

      <div class="row row-space-6">
        <div class="col-xs-12">
          <% if uploads.length == InstitutionBuilder::TABLES.length %>
            <%= link_to 'Generate Preview', dashboard_build_path, data: { confirm: 'Are you sure?' },
              class: "btn btn-warning btn-xs", role: "button", style: 'width: 70%'  %>
          <% else %>
            <a disabled="disabled" class="btn btn-warning btn-xs", role="button", style="width: 70%">
              Generate Preview
            </a>
          <% end %>
        </div>
      </div>

      <div class="row row-space-6">
        <div class="col-xs-12">
          <% if Version.preview_version.present? %>
            <%= link_to 'Download Preview As .csv', dashboard_export_path('Institution'),
              class: "btn btn-success btn-xs", role: "button", style: 'width: 70%'  %>
          <% else %>
            <a disabled="disabled" class="btn btn-warning btn-xs", role="button", style="width: 70%">
              Download Preview As .csv
            </a>
          <% end %>
        </div>
      </div>

      <div class="row row-space-6">
        <div class="col-xs-12">
          <%= link_to 'Publish Preview to Production', dashboard_push_path, data: { confirm: 'Are you sure?' },
            class: "btn btn-danger btn-xs", role: "button", style: 'width: 70%'  %>
        </div>
      </div>
    <% end %>
  </div>
</div>
