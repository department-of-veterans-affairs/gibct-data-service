<div class="row row-space-6">
  <div class="col-xs-9">
    <p class="lead">
      These are the CSV files from which the database will be constructed.
      As the CSV is uploaded, a validation on every in the CSV will ensure
      that there are no issues with the data therein. If successful, the
      newly upload data can be used to construct the Data.csv. If there
      are any issues, error messages detailing the row(s) in the CSV causing
      those errors will be output.
    </p>
  </div>

  <div class="col-xs-3">
    <% if action_name == 'new' %>
      <div class="row row-space-6">
        <div class="col-xs-12">
          <%= link_to 'Uploads List', uploads_path,
              class: "btn btn-success btn-xs", role: "button", style: 'width: 70%' %>
        </div>
      </div>
    <% else %>
      <div class="row row-space-6">
        <div class="col-xs-12">
           <%= link_to 'New Upload', new_upload_path,
               class: "btn btn-success btn-xs", role: "button", style: 'width: 70%' %>
        </div>
      </div>
    <% end %>

    <div class="row row-space-6">
      <div class="col-xs-12">
        <%= link_to 'Return to Dashboard', dashboards_path,
            class: "btn btn-info btn-xs", style: 'width: 70%' %>
      </div>
    </div>
  </div>
</div>
